#
# this file includes notes re features being working upon or requested features from the userbase that have
# been accepted and awaitng implementation.
#

Improve allstream (first pass) code to try ensure most jobs do include all streams included with original source file
and only discard bits on massive failure
@ozmartian - 24/10/2017


It would be great to have the option to add chapter markers at the beginning of each clip when the clips are combined
into one video. There could be an option to label each clip to act as the chapter name or clip name.
@Rob1050 - 24/10/2017
